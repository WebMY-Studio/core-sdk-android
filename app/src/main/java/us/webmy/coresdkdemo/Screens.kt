package us.webmy.coresdkdemo

import androidx.navigation3.runtime.NavKey

/** Back stack keys. Screen arguments (if any) are properties of the key itself. */
data object HomeKey : NavKey

data object ThemeDemoKey : NavKey
